export function scanner(host, src, callback) {
	var names = scan(host);

	callback(host);

	for(n in names) {
		if(names[n] != src) {
			scanner(names[n], host, callback);
		}
	}
}

/* vim: ft=javascript */
